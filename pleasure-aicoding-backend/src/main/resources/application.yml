spring:
  application:
    name: pleasure-AICoding
  # session 配置
  session:
    store-type: redis
    # session 30天过期
    timeout: 2592000
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/pleasure_aicoding
    username: root
    password: YJH6065hz
  # Redis
  data:
    redis:
      host: localhost
      port: 6379
      password:
      ttl: 3600
      database: 0
  profiles:
    active: local
server:
  port: 8123
  servlet:
    context-path: /api
    # 前端cookie 30 天过期
    session:
      cookie:
        max-age: 2592000
# springdoc-openapi
springdoc:
  group-configs:
    - group: 'default'
      packages-to-scan: com.pleasure.pleasureaicoding.controller
# knife4j
knife4j:
  enable: true
  setting:
    language: zh_cn
